
// TODO: 请为以下变量添加正确的类型注解，使其通过类型检查

let userName: string = "Alice";
let userAge: number = 25;
let isStudent: boolean = true;
let favoriteColor: null = null;
let currentJob: undefined = undefined;

// 测试代码 - 请不要修改以下内容
function testTypes(): void {
    console.log(`Name: ${userName}, Type: ${typeof userName}`);
    console.log(`Age: ${userAge}, Type: ${typeof userAge}`);
    console.log(`Is Student: ${isStudent}, Type: ${typeof isStudent}`);
    console.log(`Favorite Color: ${favoriteColor}, Type: ${typeof favoriteColor}`);
    console.log(`Current Job: ${currentJob}, Type: ${typeof currentJob}`);
}

testTypes();
